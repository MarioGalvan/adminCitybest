import * as types from "./types";

const initialState = {
  currentRol: {}
};

export default function rolesReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
